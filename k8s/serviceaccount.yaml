apiVersion: v1
kind: ServiceAccount
metadata:
  name: bedrock-agent-sa
  namespace: bedrock-agent
  annotations:
    # Replace with your actual IAM role ARN for IRSA
    # This role should have bedrock:InvokeModel permissions
    eks.amazonaws.com/role-arn: arn:aws:iam::YOUR_ACCOUNT_ID:role/bedrock-agent-role
